---
plan: <relative path to the plan/spec file this contract belongs to>
stage: <N>
item_id: stage-<N>
generator_role: <role-agent that drafted this contract, e.g. mb-backend>
created: <ISO-8601 UTC, e.g. 2026-07-05T12:34:56Z>
status: draft
contract_version: 1
---

# Contract: <stage title>

## In scope (what THIS item delivers)
- <concrete, testable deliverable>

## Plan of attack (ordered, mechanical)
1. <first mechanical step>
2. <next mechanical step>

## Test plan
- Unit: <what gets a unit test>
- Integration: <what gets an integration test>
- E2E (if applicable): <what gets an e2e test, or "None">

## DoD checkpoints (echoes plan, with how-to-verify)
- [ ] <DoD item 1, verbatim from the plan/task> → verified by <test or check>
- [ ] <DoD item 2, verbatim from the plan/task> → verified by <test or check>

## Out of scope (explicit non-deliverables)
- <what this item deliberately does NOT do>

## Open risks (acknowledged at contract time)
- <risk, or "None">

<!--
Reference template only (work-loop-v2 design.md §4). The real contract is
generated by scripts/mb-work-contract.sh create, which fills:
  plan / stage / item_id : from --plan / --stage
  generator_role          : from --role (default "unassigned")
  created                 : date -u +%FT%TZ at creation time
  status                  : "draft" until the reviewer (review_mode: contract)
                            approves it, then "approved"; a later revision
                            supersedes the file with status "superseded"
  # Contract: <title>     : from --title, else "<plan-topic> stage <N>"
All six body sections below the frontmatter are REQUIRED verbatim (the
scope-lock `mb-work-contract.sh validate` checks for): In scope, Plan of
attack, Test plan, DoD checkpoints, Out of scope, Open risks.
-->
