# Core-cap dogfood — живой прогон на банке этого репозитория

**Дата:** 2026-09-10 · **Стадия:** Sprint 1 Stage 7 (`mb-work-cost-diet`) · **Run:** c6cdff48

Первое живое исполнение цепочки `mb-core-cap.sh fix` → MB Manager `actualize --strict` → `check`
на настоящем банке. Цель — проверить не «скрипт работает», а инвариант AGR-043: **из core-файлов
ничего не исчезает без дословной копии в `progress.md`, живые задачи не режутся никогда.**

## Числа

| Файл | До | После `fix` | После `actualize --strict` | Кап |
|---|---|---|---|---|
| `status.md` | 223 | 157 | **55** | 60 |
| `checklist.md` | 99 | 99 | 99 | 100 |

`fix` (детерминированная ротация + компакция v2) архивировал 6 датированных разделов и честно
вернул exit 1 с текстом «still over cap — dispatch MB Manager `actualize --strict`». Он **не**
дорезал файл до капа: это и есть проектное поведение — механическая часть делает только то, что
умеет доказать, остальное эскалирует.

`actualize --strict` (Sonnet, один диспатч) архивировал ещё 10 блоков, каждый — с подтверждением
`grep -qxF` заголовка в `progress.md` **до** удаления из `status.md`.

## Проверка инварианта (оркестратором, независимо от отчёта сабагента)

- `git diff --numstat .memory-bank/progress.md` → **173 вставки, 0 удалений**. Append-only соблюдён.
- Блоков `## [status archive]` в `progress.md`: **16** (6 от ротации + 10 от актуализации).
- Маркерные блоки `<!-- mb-active-plans -->` / `<!-- mb-recent-done -->` целы (4 маркера).
- `checklist.md` побайтово не изменён этой стадией (`git diff` пуст) — прунер подтверждённо no-op.
- **Множество открытых пунктов сверено, а не счёт.** Наивное сравнение чисел вводит в заблуждение:
  формат v2 переписывает `## Stage N: <title>` + `- ⬜ <title>` в одну строку `- ⬜ Stage N — <title>`,
  поэтому счётчики до и после несравнимы напрямую. Посимвольная сверка 19 прежних открытых пунктов:
  17 на месте, 1 закрыт легитимно (Stage 6 `mb-coord.sh active`), 1 — Stage 5, переименованный
  владельцем в плане («Прунер checklist.md под реальную структуру» → «`checklist.md` v2 — один блок
  на план») и затем закрытый. **Потерянных нет.**

## Что сабагент НЕ делал (и правильно)

Контракт запрещает закрывать, ставить на паузу и приоритизировать планы. Вместо этого — список
предложений владельцу:

- `plans/2026-07-18_fix_spec-group-round3-remediation.md` — 0 открытых пунктов, в чеклисте 6/6 ✅ →
  кандидат на `mb-plan-done.sh`.
- `reviewer-v2` и `work-loop-v2` — wrapper-планы числятся `queued` с открытыми чекбоксами, тогда как
  соответствующие спеки в `roadmap.md § Linked Specs` уже 100% done; похоже, забыты при переходе на
  SDD-путь.
- `cost-multi-model`, `pi-compatibility-remediation`, `skill-improvements-anthropic-audit` — не
  тронуты более 30 дней, открытые пункты есть, признаков суперседа не найдено.
- **I-045…I-060** (Pi/OpenCode cross-agent-parity, 2026-05-24) из архивированного `## Open backlog`
  не заведены в `backlog.md` ни под этими, ни под другими ID. Сабагент сознательно НЕ завёл 16 новых
  идей вслепую: после AGR-012/013/014 подход сменился на host-native расширения, и неясно, актуальны
  ли они. Текст сохранён дословно в `progress.md`, решение — за владельцем.

## Найденное предсуществующее рассогласование (не внесено этим прогоном)

Блок `<!-- mb-active-plans -->` в `status.md` содержит 9 ссылок на файлы, физически лежащие в
`plans/superseded/` (goal-driven-autopilot sprints 1–7 + phase, opencode-first-adaptation). Блок
принадлежит `mb-roadmap-sync.sh` и не был актуализирован после переноса. Руками не правился —
подтверждено `git diff`.

## Итог

`bash scripts/mb-core-cap.sh check --mb .memory-bank` → `status_lines=55 status_cap=60
checklist_lines=99 checklist_cap=100 over=none`, **rc=0**. `mb-drift.sh` → `drift_check_core_cap=ok`.
