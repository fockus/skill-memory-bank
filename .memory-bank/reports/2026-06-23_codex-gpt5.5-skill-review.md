# Codex / GPT-5.5 review — skill-memory-bank (2026-06-23)

9 independent read-only codex (gpt-5.5, xhigh) sessions: 6 aspects + 3 transports.
Driver: `/tmp/mb-rev/run.sh`. Each finding: file:line + [BLOCKER|MAJOR|MINOR|NIT] + fix.
Backlog: I-082..I-086. Plans: `plans/2026-06-23_*.md`.

---

## 01-functionality

[MEMORY BANK: ACTIVE]

- [scripts/mb-work-range.sh:151](/Users/fockus/Apps/skill-memory-bank/scripts/mb-work-range.sh:151) + [scripts/mb-work-plan.sh:285](/Users/fockus/Apps/skill-memory-bank/scripts/mb-work-plan.sh:285) [BLOCKER] `--range N` can resolve to empty when markers have gaps, then `mb-work-plan.sh` treats empty as “all items”. This can execute the whole plan instead of the requested item. Fix: `mb-work-range.sh` must exit non-zero when requested range emits no existing item; `mb-work-plan.sh` must only default to all when no range was requested.

- [hooks/mb-plan-sync-post-write.sh:53](/Users/fockus/Apps/skill-memory-bank/hooks/mb-plan-sync-post-write.sh:53) [MAJOR] Post-write sync calls `mb-plan-sync.sh` with no required `<plan-file>`; `mb-plan-sync.sh` requires it at [scripts/mb-plan-sync.sh:26](/Users/fockus/Apps/skill-memory-bank/scripts/mb-plan-sync.sh:26). The hook therefore warns and does not sync the written plan. Fix: pass `FILE_PATH` for plan writes, resolve the bank, and pass that bank to roadmap/traceability steps.

- [scripts/mb-work-protected-check.sh:135](/Users/fockus/Apps/skill-memory-bank/scripts/mb-work-protected-check.sh:135) [MAJOR] Protected globs are matched against raw paths and basename only; absolute paths bypass directory globs like `.github/workflows/**` and `ci/**`. Verified: relative `.github/workflows/test.yml` matches, absolute `/Users/.../.github/workflows/test.yml` returns rc 0. Fix: normalize file paths relative to project root/bank before glob matching.

- [scripts/mb-work-plan.sh:319](/Users/fockus/Apps/skill-memory-bank/scripts/mb-work-plan.sh:319) [MAJOR] The capability-aware dispatcher is not wired into the work spine; `mb-work-plan.sh` reads `agent/model/thinking` directly from YAML and no production script calls `mb-agent-caps.sh`. Fix: make the work executor resolve each role through `mb-agent-caps.sh resolve`, or remove the advertised dispatcher path.

- [references/pipeline.default.yaml:10](/Users/fockus/Apps/skill-memory-bank/references/pipeline.default.yaml:10) + [scripts/mb-agent-caps.sh:210](/Users/fockus/Apps/skill-memory-bank/scripts/mb-agent-caps.sh:210) [MAJOR] Fresh default pipeline has roles without `model`, but `mb-agent-caps.sh resolve` treats missing model as fatal. Verified with default fallback: `role 'developer' has no model`, rc 1. Fix: add default role models or let resolver use tier fallback before failing unknown roles.

- [scripts/mb-deps-check.sh:7](/Users/fockus/Apps/skill-memory-bank/scripts/mb-deps-check.sh:7) + [scripts/mb-workflow.sh:81](/Users/fockus/Apps/skill-memory-bank/scripts/mb-workflow.sh:81) [MAJOR] Dependency contract is inconsistent: PyYAML is reported optional, but workflow resolution exits if `yaml` is missing. Fix: either mark PyYAML required or add the same minimal YAML fallback used elsewhere.

- [scripts/mb-subinvoke-resolve.sh:114](/Users/fockus/Apps/skill-memory-bank/scripts/mb-subinvoke-resolve.sh:114) [MAJOR] Fan-out auto subinvoke supports only `codex` and `claude-code`; `pi`/`opencode` fail loud unless the operator supplies `--cmd`, despite the dispatcher story naming them as target transports. Fix: add tested `pi` and `opencode` templates or resolve from pipeline/adapter config.

- [hooks/lib/session-common.sh:7](/Users/fockus/Apps/skill-memory-bank/hooks/lib/session-common.sh:7) + [hooks/mb-session-turn.sh:31](/Users/fockus/Apps/skill-memory-bank/hooks/mb-session-turn.sh:31) [MAJOR] Session capture uses `sc_resolve_mb`, which only walks for local `.memory-bank`; it ignores `MB_PATH` and global registry. Global-storage projects silently lose session memory. Fix: use `_skill_root.sh::mb_hook_resolve_mb_path` or make `sc_resolve_mb` registry-aware.

- [scripts/mb-work-resolve.sh:124](/Users/fockus/Apps/skill-memory-bank/scripts/mb-work-resolve.sh:124) [MINOR] Bank-relative targets like `specs/dynamic-flow/tasks.md` or `specs/dynamic-flow` fail, although roadmap/status expose paths in that form. Fix: before topic sanitization, resolve `plans/*` and `specs/*` relative to `BANK`.

- [hooks/_skill_root.sh:110](/Users/fockus/Apps/skill-memory-bank/hooks/_skill_root.sh:110) [MINOR] Registry lookup builds a `bash -c` string with single-quoted interpolated paths; a project path containing `'` breaks global bank resolution. Fix: pass `lib`, `agent`, and project root as positional args to `bash -c` instead of interpolating.

VERDICT: NO-GO for end-to-end operability; fix first: 1) range-empty executes all, 2) protected absolute path bypass, 3) dispatcher not wired/default models absent.
---

## 02-logic

[MEMORY BANK: ACTIVE]

- `scripts/mb-agent-caps.sh:210` [BLOCKER] Default `references/pipeline.default.yaml` roles have no `model`, so `resolve --role developer` exits 1 instead of using fallback. Fix: add default role models or allow empty contract to fall back to `claude-agent`.

- `scripts/mb-agent-caps.sh:124` [MAJOR] `roles.<role>.agent` is ignored. Current `agent: codex-cli, model: gpt-5.5` can resolve to `claude-agent/opus`. Fix: map transport-like agents (`codex-cli`, `codex`, `pi`, `opencode`) into preferred transport before global priority.

- `scripts/mb-agent-caps.sh:121` [MAJOR] YAML load/parse errors are swallowed as `{}`, producing misleading “role has no model” exit 1, not parse failure exit 2. Fix: emit parse error and return 2.

- `scripts/mb-flow-route.sh:428` [MAJOR] Route-floor misses lowercase contract/interface/protocol filenames like `src/user_interface.py`, `src/api_contract.py`, `src/user_protocol.py`; verified route stays `code-change`. Fix: normalize lowercase basename/path and match `*interface*`, `*contract*`, `*protocol*`, `*abc*` with tests.

- `scripts/mb-flow-route.sh:390` [MAJOR] Auto changed-file detection ignores untracked files, so a new untracked `domain/` or contract file will not force `arch`. Fix: include `git ls-files --others --exclude-standard`.

- `scripts/mb-work-plan.sh:108` [MAJOR] Wrapper frontmatter is parsed with regex; `tasks: 1-3 # comment` or quoted `linked_spec` is missed, causing all spec tasks to run. Fix: parse YAML frontmatter or implement comment-aware scalar parsing.

- `scripts/mb-conflicts.sh:342` [MAJOR] `base64 --decode` is not portable to macOS BSD base64; `|| true` masks failure and sends empty bodies to the judge. Fix: decode via Python stdlib or `base64 -d || base64 -D`.

- `scripts/mb-fanout.sh:393` [MINOR] Branch stderr is discarded, so failed parallel branches lose the concrete error and aggregate only says `exit N`. Fix: capture `err.<i>` and include a truncated stderr snippet in the error marker.

- `scripts/mb-conflicts.sh:81` [MINOR] `--threshold nan`/`inf` is accepted; `nan` makes comparisons behave incorrectly and can flag nearly every marker pair. Fix: require finite `0 <= threshold <= 1`, else exit 64.

VERDICT: NO-GO for logical correctness until dispatcher defaults and route-floor false negatives are fixed.

Top 3 to fix first:
1. `mb-agent-caps.sh` default/per-project transport resolution.
2. `mb-flow-route.sh` false negatives for lowercase contract/interface/protocol and untracked files.
3. `mb-work-plan.sh` YAML frontmatter parsing for wrapper `linked_spec` / `tasks`.
---

## 03-consistency

[MEMORY BANK: ACTIVE]

- [MAJOR] [README.md](/Users/fockus/Apps/skill-memory-bank/README.md:39) says `25` slash commands, while [README.md](/Users/fockus/Apps/skill-memory-bank/README.md:259) says `29`; the command table at [README.md](/Users/fockus/Apps/skill-memory-bank/README.md:263) omits `/analyze-task`, `/flow`, `/goal`, which exist in [commands/analyze-task.md](/Users/fockus/Apps/skill-memory-bank/commands/analyze-task.md:6), [commands/flow.md](/Users/fockus/Apps/skill-memory-bank/commands/flow.md:6), [commands/goal.md](/Users/fockus/Apps/skill-memory-bank/commands/goal.md:6). Fix: update count/table from `commands/*.md` and add a generated consistency check.

- [MAJOR] `/mb reindex` is documented in [README.md](/Users/fockus/Apps/skill-memory-bank/README.md:312) and [SKILL.md](/Users/fockus/Apps/skill-memory-bank/SKILL.md:290), but the `/mb` router table in [commands/mb.md](/Users/fockus/Apps/skill-memory-bank/commands/mb.md:25) does not expose it; actual implementation is only a hook helper, [hooks/mb-reindex.sh](/Users/fockus/Apps/skill-memory-bank/hooks/mb-reindex.sh:3). Fix: either add `/mb reindex` routing/docs or remove it from public command docs.

- [MAJOR] [references/hooks.md](/Users/fockus/Apps/skill-memory-bank/references/hooks.md:3) describes only “first five entries” and repeats “all five” at [references/hooks.md](/Users/fockus/Apps/skill-memory-bank/references/hooks.md:188), but actual config includes many lifecycle hooks, e.g. [settings/hooks.json](/Users/fockus/Apps/skill-memory-bank/settings/hooks.json:92), [settings/hooks.json](/Users/fockus/Apps/skill-memory-bank/settings/hooks.json:106), [settings/hooks.json](/Users/fockus/Apps/skill-memory-bank/settings/hooks.json:124). Fix: regenerate this reference from `settings/hooks.json` and split tool hooks vs lifecycle hooks.

- [MINOR] Graph docs are inconsistent: summary omits `--docs` and `--sessions` at [commands/mb.md](/Users/fockus/Apps/skill-memory-bank/commands/mb.md:52), while script supports both at [scripts/mb-codegraph.py](/Users/fockus/Apps/skill-memory-bank/scripts/mb-codegraph.py:483) and [scripts/mb-codegraph.py](/Users/fockus/Apps/skill-memory-bank/scripts/mb-codegraph.py:489); README documents `--sessions` at [README.md](/Users/fockus/Apps/skill-memory-bank/README.md:306). Fix: align all `/mb graph` flag docs.

- [MINOR] [commands/work.md](/Users/fockus/Apps/skill-memory-bank/commands/work.md:12) still calls `--slim`, `--full`, context-slim hooks, and reviewer detection “Phase 4”, but these are implemented/configured in [settings/hooks.json](/Users/fockus/Apps/skill-memory-bank/settings/hooks.json:49), [hooks/mb-context-slim-pre-agent.sh](/Users/fockus/Apps/skill-memory-bank/hooks/mb-context-slim-pre-agent.sh:5), and [install.sh](/Users/fockus/Apps/skill-memory-bank/install.sh:886). Fix: rewrite as current behavior and correct stale hook names.

- [MINOR] `/mb help` says “18 subcommands” at [commands/mb.md](/Users/fockus/Apps/skill-memory-bank/commands/mb.md:929), but the router table has more entries, including Dynamic Flow at [commands/mb.md](/Users/fockus/Apps/skill-memory-bank/commands/mb.md:63). Fix: remove the hard-coded count or generate it.

- [MINOR] [commands/done.md](/Users/fockus/Apps/skill-memory-bank/commands/done.md:42) says “6-step flow”, but lists 8 steps at [commands/done.md](/Users/fockus/Apps/skill-memory-bank/commands/done.md:44). Fix: update the count or avoid counting.

- [MINOR] [SKILL.md](/Users/fockus/Apps/skill-memory-bank/SKILL.md:219) documents test-runner JSON without `tests_failed`, but [scripts/mb-test-run.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-test-run.sh:202) emits it and [commands/test.md](/Users/fockus/Apps/skill-memory-bank/commands/test.md:20) documents it. Fix: add `tests_failed` to `SKILL.md`.

- [NIT] [commands/work.md](/Users/fockus/Apps/skill-memory-bank/commands/work.md:472) documents `max-cycles` without `--`, but parser expects `--max-cycles` in [scripts/mb-workflow.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-workflow.sh:58). Fix: correct the synopsis.

- [NIT] [references/structure.md](/Users/fockus/Apps/skill-memory-bank/references/structure.md:269) says `god-nodes.md` is Top-20 by degree, but analytics ranks by PageRank when available at [memory_bank_skill/codegraph_analytics.py](/Users/fockus/Apps/skill-memory-bank/memory_bank_skill/codegraph_analytics.py:301). Fix: document PageRank primary, degree fallback/secondary.

VERDICT: есть заметный doc-vs-code drift, релизить публичные docs без выравнивания команд, hooks reference и `/mb work` нельзя.

Top 3 first: fix README command inventory; decide and wire/remove `/mb reindex`; regenerate `references/hooks.md` from actual hooks config.
---

## 04-gaps

[MEMORY BANK: ACTIVE]

- `scripts/mb-done-gates.sh:227` / `scripts/mb-done-gates.sh:261` [BLOCKER] `/mb done` silently accepts runner crashes, malformed JSON, and `tests_pass=null` because runner output is captured with `|| true` and `null` is downgraded to WARN/pass. Fix: fail the gate when runner exits non-zero, output is invalid JSON, or `tests_pass` is `null` without explicit `not_applicable=true`; add bats tests for each path.

- `scripts/mb-test-run.sh:10` / `scripts/mb-test-run.sh:84` [BLOCKER] test runner only supports Python and Go but exits 0 for unsupported stacks, while this repo’s required checks include bats. A project can pass `/mb done` with failing shell tests. Fix: either use `_lib.sh` stack test detection consistently or require explicit `test_command`; add coverage for bats, node/rust fallback, no pytest, and failing command.

- `.github/workflows/test.yml:44` / `hooks/tests/session-turn.bats:1` [MAJOR] tracked hook tests under `hooks/tests/` are not run in CI; CI only runs `tests/bats`, `tests/e2e`, and `tests/pytest`. Fix: add `bats hooks/tests/*.bats` and `pytest hooks/tests/*_test.py` to CI and README verification commands.

- `scripts/mb-agent-caps.sh:118` [MAJOR] malformed `pipeline.yaml` is swallowed as `{}`, contradicting the documented parse-failure exit code at `scripts/mb-agent-caps.sh:31`. Fix: emit the YAML error and exit 2; add a bats test with invalid YAML.

- `scripts/mb-handoff.sh:35` / `scripts/mb-flow-sync.sh:56` / `scripts/_lib.sh:66` [MAJOR] BSD-first `stat -f %m || stat -c %Y` is known-broken on GNU stat when `stat -f` exits 0 with non-numeric output; one hook has a regression test, these helpers do not. Fix: centralize numeric mtime detection or use GNU-first with validation; add stat-shim tests for all call sites.

- `scripts/mb-import.py:11` / `tests/pytest/test_import.py:6` [MAJOR] import contract mentions `lessons.md` and `status.md`, but implementation writes only progress, notes, and state. This is documented as TODO while tests do not assert the missing behavior. Fix: implement lessons/status import with pytest coverage or remove it from the advertised contract.

- `scripts/mb-upgrade.sh:117` / `tests/bats/test_upgrade.bats:4` [MAJOR] release-critical fetch/pull/apply paths are explicitly untested; only preflight guards are covered. Fix: add local bare-remote tests for up-to-date, behind, ahead/diverged, missing remote branch, `--check`, `--force`, and failed `git pull`.

- `scripts/mb-index.sh:13` / `commands/mb.md:313` [MINOR] public `/mb index` script has no tests, no `--help`, and still prints `[MEMORY BANK: INACTIVE]` instead of the current `[MEMORY BANK: ABSENT]` state. Fix: add bats coverage for no bank, core sections, notes/plans/report counts, path-with-spaces, `--help`, and update wording.

- `.github/workflows/test.yml:77` [MINOR] static checks omit important surfaces: shellcheck skips `hooks/lib/*.sh`, adapters, install scripts; ruff skips `scripts/*.py`, `hooks/lib/*.py`, and `memory_bank_skill/`. Fix: expand shellcheck/ruff targets and add failures to CI.

VERDICT: не готово как надежный developer toolkit; главные риски в том, что verification gates могут ложно пропускать незапущенные или упавшие проверки.

Top 3 first: 1. Сделать `/mb done` fail-closed для runner errors/null/malformed JSON. 2. Запускать весь реальный test surface, включая bats и `hooks/tests`, в runner и CI. 3. Исправить silent YAML parse failure в `mb-agent-caps.sh` и добавить регрессионный тест.
---

## 05-security

[MEMORY BANK: ACTIVE]
- `hooks/_skill_root.sh:110` [BLOCKER] Эксплуатируемость: высокая. `cwd`/`MB_PROJECT_ROOT`/`agent` вставляются в `bash -c` строку без shell-quoting; путь проекта с `' ; cmd ; #` даст command injection при срабатывании hook. Fix: не строить shell-string; source `_lib.sh` в текущем shell и вызвать `mb_registry_lookup "$agent" "$project_root"` с обычными quoted args.

- `scripts/mb-plan-done.sh:379` [BLOCKER] Эксплуатируемость: высокая. Репозиторий может положить `.memory-bank/.mbenv`, и скрипт без подтверждения выполнит его через `.`. Fix: заменить `source` на whitelist parser для допустимых `KEY=value` (`MB_TEST_ROOTS` и т.п.), без выполнения shell.

- `scripts/mb-flow-sync.sh:130`, `scripts/mb-handoff.sh:111` [MAJOR] Эксплуатируемость: высокая. Trap собирается строкой с подставленным путём lock; путь банка с одинарной кавычкой ломает quoting и может выполнить команды на `EXIT`. Fix: cleanup/release function + globals, `trap _cleanup EXIT`, без интерполяции значений в trap-string.

- `scripts/mb-work-resolve.sh:51`, `scripts/mb-work-resolve.sh:110` [MAJOR] Эксплуатируемость: высокая. Active plan link из `roadmap.md` может быть `../../...`; скрипт realpath’ит и возвращает внешний файл. Это превращает bank-controlled markdown в read/exfiltration primitive для `/mb work`. Fix: принимать только canonical paths внутри `$BANK/plans/*.md` или `$BANK/specs/*/tasks.md`; запретить absolute/`..`.

- `scripts/mb-pipeline.sh:130`, `scripts/mb-pipeline.sh:169` [MAJOR] Эксплуатируемость: средняя-высокая. `MB_PIPELINE` и `.mb-config pipeline=` не проходят `valid_pipeline_name`, поэтому `../../x` выбирает YAML вне `<bank>/pipelines`. Это может обойти `protected_paths`/workflow gates. Fix: валидировать имя до path join и проверять realpath внутри `$mb/pipelines`.

- `scripts/mb-work-protected-check.sh:138` [MAJOR] Эксплуатируемость: высокая. Absolute paths не нормализуются относительно repo root: `/repo/ci/deploy.yml` не матчится с `ci/**`; basename fallback ловит только `.env`/`Dockerfile`, но не nested protected dirs. Fix: canonicalize candidate path, derive repo root, match both repo-relative and basename.

- `hooks/mb-protected-paths-guard.sh:31`, `hooks/block-dangerous.sh:67` [MAJOR] Эксплуатируемость: высокая. Protected-path guard покрывает только `Write|Edit`; Bash redirections/`tee`/`sed -i` в `.env`, `ci/**`, `.pem` обходят его. Fix: добавить Bash PreToolUse protected-path parser или пропускать извлечённые write targets через `mb-work-protected-check.sh`.

- `hooks/mb-session-turn.sh:128`, `hooks/lib/session-common.sh:140` [MAJOR] Эксплуатируемость: высокая. `<private>...</private>` не удаляется перед записью live-log bullet в `session/*.md`; редактируются только token/API-key паттерны. Fix: общий sanitizer должен strip’ать `<private>` blocks до persist и до LLM summary, как `semantic_chunk.py`.

- `hooks/file-change-log.sh:127` [MAJOR] Эксплуатируемость: средняя. Secret warning печатает всю строку секрета в stderr (`echo "$SECRETS"`), что может попасть в transcript/log. Fix: выводить только `file:line` и имя переменной, значение заменять на `[REDACTED]`.

- `scripts/mb-context.sh:39` [MAJOR] Эксплуатируемость: высокая. Core files читаются через `[[ -f ]]` + `cat`, что следует symlink’ам; malicious repo может сделать `.memory-bank/status.md -> ~/.ssh/config` и утянуть содержимое в context. Fix: reject symlinks, canonicalize realpath under bank, читать только regular files owned by bank.

- `scripts/mb-search.sh:107`, `scripts/mb-search.sh:127` [MAJOR] Эксплуатируемость: средняя. Tag mode доверяет `index.json` path и делает `head "$MB_PATH/$rel"`; malicious/stale index может указать `../../...`. Fix: canonicalize each indexed path under `$MB_PATH`, regenerate or reject untrusted index entries.

VERDICT: FAIL — security hardening required before release.

Top 3 issues to fix first: `hooks/_skill_root.sh` command injection; `.memory-bank/.mbenv` arbitrary code execution; all path/trap interpolation and bank path traversal guards.
---

## 06-configurability

[MEMORY BANK: ACTIVE]

- [BLOCKER] [scripts/mb-agent-caps.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-agent-caps.sh:89): resolver моделей обходит `mb-pipeline.sh path`, поэтому игнорирует `MB_PIPELINE`, host binding и `<bank>/pipelines/*.yaml`; workflow может идти по named pipeline, а transport/model — по legacy `<bank>/pipeline.yaml`. Fix: резолвить pipeline только через `mb-pipeline.sh path` и покрыть `MB_PIPELINE`/host-binding тестами для `mb-agent-caps`.

- [MAJOR] [scripts/mb-reviewer-resolve.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-reviewer-resolve.sh:47): reviewer resolver имеет тот же старый ladder `<bank>/pipeline.yaml → default`, хотя docs обещают named pipelines. Fix: заменить ручной выбор на `mb-pipeline.sh path`; обновить help-комментарий.

- [MAJOR] [scripts/mb-pipeline-validate.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-pipeline-validate.sh:442): validator почти не проверяет runtime-блоки `review`, `judge`, `review_ensemble`, `done_gates`, `done_placeholders`, `dispatch`; плохие enum/type значения проходят до runtime. Fix: добавить schema checks для всех top-level knobs, особенно `dispatch.priority/on_none_available/enumerable/prefer/model_map/fallback` и `<stage>.enabled`.

- [MAJOR] [.memory-bank/pipeline.yaml](/Users/fockus/Apps/skill-memory-bank/.memory-bank/pipeline.yaml:34): duplicate YAML key `judge` повторяется на line 41, а `mb-pipeline.sh validate .memory-bank` возвращает `0`; PyYAML молча оставляет последнее значение. Fix: использовать YAML loader, который запрещает duplicate keys, в validator и runtime parsers.

- [MAJOR] [references/pipeline.default.yaml](/Users/fockus/Apps/skill-memory-bank/references/pipeline.default.yaml:217): `budget.default_limit` задокументирован как проектный default, но [mb-work-budget.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-work-budget.sh:41) читает только `warn_at_percent/stop_at_percent`, а [commands/work.md](/Users/fockus/Apps/skill-memory-bank/commands/work.md:298) инициализирует budget только при `--budget`. Fix: если CLI budget не задан, применять non-null `budget.default_limit`.

- [MAJOR] [scripts/mb-agent-caps.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-agent-caps.sh:210): default pipeline unusable for caps: roles in [pipeline.default.yaml](/Users/fockus/Apps/skill-memory-bank/references/pipeline.default.yaml:10) have no `model`, so `resolve --role reviewer` exits with “has no model”, despite fallback model defaults at lines 148-155. Fix: either add contract models to default roles, or allow empty contract to fall back to `claude-agent` tier defaults.

- [MAJOR] [scripts/mb-agent-caps.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-agent-caps.sh:81): `codex` has a model probe at line 63, but default `dispatch.enumerable` excludes it, so unsupported Codex models are trusted blindly. Fix: include `codex` in enumerable when probe works, or remove the probe and document explicit `model_map` requirement.

- [MINOR] [scripts/mb-config.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-config.sh:9): `mb-config` only supports `lang`, while [mb-pipeline.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-pipeline.sh:96) stores `pipeline=<name>` in the same `.mb-config`; config ownership is split and unclear. Fix: either add `pipeline` get/set/validate to `mb-config`, or rename this script to locale-specific.

- [MINOR] [memory_bank_skill/rules_profile.py](/Users/fockus/Apps/skill-memory-bank/memory_bank_skill/rules_profile.py:125): profile validation checks role/stack/architecture/delivery/strictness but not `scope`, although docs restrict it to `user|project`. Fix: validate `scope` and reject unknown values.

- [MINOR] [scripts/mb-rules-check.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-rules-check.sh:39): rule profiles created by `mb-profile.sh init --scope=project` are not auto-consumed; rules-check only reads `MB_PROFILE`/`--profile`. Fix: default to `<resolved-mb>/rules-profile.json` plus user profile resolution, with explicit CLI override winning.

VERDICT: NOT SAFE for configurable workflows/transports/models yet.

Top 3 first: unify all pipeline consumers through `mb-pipeline.sh path`; extend validator to cover runtime config + duplicate YAML keys; make budget/model fallback defaults actually executable.
---

## 07-transport-codex

[MEMORY BANK: ACTIVE]

- [MAJOR] `references/pipeline.default.yaml:292` / `references/pipeline.default.yaml:298` / `references/pipeline.default.yaml:305`: shipped default does not route Codex-family models to Codex. `priority` excludes `codex`, `prefer` is `{}`, and `model_map` is `{}`, so `openai-codex/*`, `chatgpt*`, or `gpt-*` only route to Codex if every project adds overrides. Fix: ship real defaults, e.g. `prefer: {"openai-codex/*": codex, "gpt-*": codex}` and `model_map: {"openai-codex/gpt-5.5": {codex: gpt-5.5}}`.

- [MAJOR] `.memory-bank/pipeline.yaml:29`: per-project reviewer declares `agent: codex-cli` + `model: gpt-5.5`, but `scripts/mb-agent-caps.sh:124` only reads `roles.<role>.model` and `dispatch.*`; it ignores `agent`, and this project has no `dispatch` block. Result: installed Codex CLI is not preferred through `mb-agent-caps`; it falls toward `pi/opencode/claude-agent`. Fix: either add project `dispatch.prefer: {"gpt-*": codex}` or make the resolver translate `agent: codex-cli` into `prefer=codex`.

- [MAJOR] `scripts/mb-agent-caps.sh:63`: `caps_models codex` calls `codex --list-models` or `codex models`, but current Codex CLI documents `codex debug models`, not those commands; local `codex --help` also has no `models` command. Since Codex is trusted/non-enumerable, this should not probe models at all in normal detect, or should use `codex debug models --bundled` if a catalog is explicitly requested. Fix: return empty for Codex unless `codex debug models --bundled` is supported and parsed safely.

- [MAJOR] `references/pipeline.default.yaml:10`: bundled default roles have no `model`, while `scripts/mb-agent-caps.sh:210` treats missing model as exit `1`. That makes `mb-agent-caps.sh resolve --role reviewer` unusable on the shipped default pipeline. Fix: add role model defaults, or derive a tier fallback before failing.

- [MINOR] `scripts/mb-agent-caps.sh:118`: YAML import/read/parse errors are swallowed as `data = {}`, then reported as “role has no model” with exit `1`, despite the header contract saying pipeline read/parse failure is exit `2` (`scripts/mb-agent-caps.sh:32`). Fix: on PyYAML missing or parse failure, print the parse error and exit/return `2`.

- [NIT] `scripts/mb-agent-caps.sh:72`: Codex is correctly non-enumerable/trusted by default (`CAPS_ENUMERABLE` defaults to `pi,opencode`; default YAML also lists only `pi, opencode`). No fix needed.

- [NIT] `scripts/mb-subinvoke-resolve.sh:120`: mapped Codex model id `gpt-5.5` is correct for Codex CLI; OpenAI Codex Models documents `codex -m gpt-5.5`, and `codex exec --model` is a documented flag. No fix needed.

Sources checked: [Codex Models](https://developers.openai.com/codex/models), [Codex CLI reference](https://developers.openai.com/codex/cli/reference).

VERDICT: FAIL for default/project Codex routing; the core trusted/non-enumerable mechanism is sound, but defaults do not actually select Codex.

Top 3 first: fix default `dispatch.prefer/model_map`; align `.memory-bank/pipeline.yaml` `agent: codex-cli` with resolver routing; remove/replace invalid `codex --list-models || codex models` probe.
---

## 08-transport-opencode

[MEMORY BANK: ACTIVE]

- `scripts/mb-subinvoke-resolve.sh:114` [BLOCKER] `opencode` не имеет sub-invoke ветки, а `scripts/mb-fanout.sh:204` вызывает этот resolver по default path. При `MB_AGENT=opencode` fan-out падает, хотя caps уже может выбрать `transport=opencode`. Fix: добавить `opencode` template и тест; `adapters/opencode.sh:216` тоже должен иметь `subinvoke` action.

- `scripts/mb-work-plan.sh:183` [BLOCKER] `/mb work` не использует `mb-agent-caps.sh`: он читает `roles.*.model` напрямую, а `commands/work.md:306` всё ещё говорит dispatch via `Task`. Поэтому `transport=opencode` из `mb-agent-caps.sh:231` фактически никуда не попадает. Fix: ввести реальный `mb-agent-dispatch.sh`/runner или подключить caps-resolve перед dispatch и маршрутизировать `opencode run`.

- `references/pipeline.default.yaml:10` [MAJOR] shipped default roles почти не задают `model`, но `mb-agent-caps.sh:210` требует contract model и иначе exits 1. `dispatch.model_map` при этом пустой (`references/pipeline.default.yaml:305`). Default opencode/caps path неработоспособен без project override. Fix: либо добавить role models + opencode model_map, либо сделать resolver совместимым с отсутствующим model.

- `scripts/mb-agent-caps.sh:89` [MAJOR] caps resolver вручную ищет только `<bank>/pipeline.yaml`/default и игнорирует named pipeline selection/host binding из `scripts/mb-pipeline.sh:42`. OpenCode-specific pipeline с `agents: [opencode]` не будет выбран. Fix: использовать `mb-pipeline.sh path` вместо локального `resolve_pipeline_path`.

- `scripts/mb-agent-caps.sh:59` [MAJOR] `opencode models` ошибки полностью глушатся и превращаются в “модель отсутствует”; strict gating затем молча falls through. Это смешивает реальный missing model с broken auth/cache/db. Fix: различать empty list и command failure, отдавать диагностический reason, при `on_none_available: error` падать.

- `scripts/mb-agent-caps.sh:134` [MINOR] `dispatch.enumerable: []` невозможно задать: пустой список заменяется default `[pi, opencode]`. Fix: проверять наличие ключа, а не truthiness.

VERDICT: opencode transport сейчас частично распознаётся, но не является рабочим end-to-end path.

Top 3 first: 1. подключить caps к реальному dispatch runner; 2. добавить `opencode` subinvoke/adapter action; 3. исправить default pipeline models/model_map или fallback contract.
---

## 09-transport-pi

[MEMORY BANK: ACTIVE]

- [scripts/mb-subinvoke-resolve.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-subinvoke-resolve.sh:114) [BLOCKER] `mb-agent-caps.sh` может вернуть `transport=pi`, но subinvoke resolver знает только `codex` и `claude-code`; тесты даже закрепляют `pi` как unresolved. Реальный pi transport не исполняем. Fix: добавить `pi)` template, например `pi -p --no-session --model "$MB_SUBINVOKE_MODEL" "$MB_FANOUT_PROMPT"`, с тестами на prompt-env seam и model grammar.

- [scripts/mb-work-plan.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-work-plan.sh:319) [BLOCKER] capability resolver фактически не подключён к `/mb work`: work-plan берёт `agent/model/thinking` напрямую из `roles.*`, а [commands/work.md](/Users/fockus/Apps/skill-memory-bank/commands/work.md:313) затем dispatch’ит Task с этим model. `dispatch.priority: [pi, opencode, claude-agent]` из pipeline не влияет на реальный execution path. Fix: в execution planner/dispatcher вызывать `mb-agent-caps.sh resolve --role ...`, добавить `transport` в JSONL и маршрутизировать CLI transports через subinvoke.

- [references/pipeline.default.yaml](/Users/fockus/Apps/skill-memory-bank/references/pipeline.default.yaml:10) [MAJOR] default pipeline объявляет capability-aware `dispatch`, но роли не задают `model`; [scripts/mb-agent-caps.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-agent-caps.sh:210) сразу падает `role has no model`, не доходя до pi-first priority/fall-through. Fix: либо добавить дефолтные contract model ids для ролей, либо изменить resolver: no role model → deterministic fallback to `claude-agent`/configured default, не exit 1.

- [scripts/mb-agent-caps.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-agent-caps.sh:62) [MINOR] malformed `pi --list-models` stdout парсится как модели: любая строка `NF>=2` становится `$1/$2`. Pi при `models.length === 0` пишет human text в stdout, так что `detect` может считать `No/models`. Empty output falls through, но malformed output не отличим от таблицы. Fix: начинать парсинг только после header `provider model ...`, валидировать provider/model regex, non-table stdout → zero models + diagnostic.

- [scripts/mb-agent-caps.sh](/Users/fockus/Apps/skill-memory-bank/scripts/mb-agent-caps.sh:62) [NIT] header skip работает только для `NR==1 && $1=="provider"`; если перед таблицей появится stdout-banner, header попадёт как `provider/model`. Fix: skip header by columns anywhere before data, e.g. `$1=="provider" && $2=="model"{seen=1; next}`.

VERDICT: pi transport не готов end-to-end; top 3: подключить caps resolver к `/mb work`, добавить `pi` subinvoke template, исправить default/no-model contract handling.
