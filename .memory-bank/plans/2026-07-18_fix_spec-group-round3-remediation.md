---
type: fix
topic: spec-group-round3-remediation
status: in_progress
depends_on: []
parallel_safe: false
linked_specs: [specs/sdd-vision-pipeline, specs/svp-sdd-core, specs/svp-interview-upgrade, specs/svp-roadmap-backlog-db, specs/svp-brief, specs/svp-parallel-engine, specs/svp-adapt-escalation, specs/svp-docs-wiki, specs/svp-contract-test-loop]
created: 2026-07-18
---
# Plan: fix — spec-group-round3-remediation

**Baseline commit:** b7b67c5a2a1a673b3c836b2a786f419a968f24c6

## Context

**Problem:** Круг 3 codex-ревью группы `sdd-vision-pipeline` (9 технических ревьюеров gpt-5.6-sol + смысловой аудитор, 2026-07-17) дал 9/9 CHANGES_REQUESTED: **73 технические находки** (7 critical, 60 major, 5 minor, 1 nit; из них 15 UNFIXED/PARTIAL из круга 2) + **2 смысловые** (D-14 `ice_confirmed` декоративен; Cursor выпал из D-07). Отчёт: `reports/2026-07-17_review_spec-group-round3.md`, raw-вердикты: `reports/2026-07-17_review_spec-group-round3-raw/*.json`.

**Expected result:** Все 75 находок закрыты правками спек либо отклонены с якорной цитатой-доказательством; полная батарея 9/9 спек зелёная; решения AGR-020 (Cursor в полном режиме S3) и AGR-021 (ICE: авто-приоритизация + ворнинг + эскалация подтверждения пользователю) приземлены в спеки; банк актуализирован. Группа готова к контрольному кругу 4 (по команде пользователя).

**Execution model (требование пользователя):** не больше **4 саб-агентов-фиксеров, все на Opus**, две волны по 2 параллельных (вместо 10 одновременных, как в круге 2). Правило группы: **контракт чинит владелец, потребитель выравнивается под ТЕКУЩИЙ текст владельца** — поэтому волна 1 = владельцы контрактов, волна 2 = потребители. После каждого фиксера — независимая верификация оркестратором до старта зависимой волны. Исполнение — оркестратором этой сессии через Agent-спавн (спеко-правки, не код; явный выбор пользователем упрощённого workflow вместо `/mb work`).

**Пакеты фиксеров (непересекающиеся файловые границы):**

| Фиксер | Спеки (файлы только своего пакета) | Находки | Контракты во владении |
|---|---|---|---|
| F1 owners-state | `sdd-vision-pipeline` (7) + `svp-roadmap-backlog-db` (11) | 18 техн. + D-14/AGR-021 | lock `mb_lock_acquire` (S4-C6 ↔ umbrella Interface 2), ordering (S4-C2), ICE-frontmatter (Interface 4), DAG, эскалационный журнал (interface-сторона) |
| F2 owners-norms | `svp-sdd-core` (9) + `svp-interview-upgrade` (6) | 15 техн. | нормы red-якорей (S2 REQ-054/055), work-state API, glob-грамматика C1, secret-политика транскриптов (S1), prompt-harness |
| F3 consumers-west | `svp-brief` (9) + `svp-parallel-engine` (6) + `svp-adapt-escalation` (8) | 23 техн. + AGR-020 | потребители: S7←S1, S3←S4/S2, S5←S3/S4/S2 |
| F4 consumers-east | `svp-docs-wiki` (10) + `svp-contract-test-loop` (7) | 17 техн. | потребители: S6←S4(lock)/S2, S8←S2(нормы) |

**Related files:**
- `reports/2026-07-17_review_spec-group-round3.md` — сводный отчёт (готовые правки в findings)
- `reports/2026-07-17_review_spec-group-round3-raw/` — 10 raw JSON-вердиктов (источник пакетов ТЗ)
- `specs/{sdd-vision-pipeline,svp-*}/{requirements,design,tasks}.md` + `context/{sdd-vision-pipeline,svp-*}.md` — правятся
- `context/*-interview.md` — исторические протоколы, **НЕ редактируются**
- `agreements.md` — AGR-019/020/021 (границы решений)
- `notes/2026-07-17_1125_red-anchor-pitfalls-output-signatures.md` — грабли якорей (вшивается в ТЗ)

---

## Stages

<!-- mb-stage:1 -->
### Stage 1: Пакеты находок и ТЗ для 4 фиксеров

**What to do:**
- Разрезать 10 raw-вердиктов на 4 пакета по таблице выше; каждой находке — полный JSON (id, severity, location, evidence, proposed_fix).
- Написать 4 промпта `scratchpad/review/fix3-{f1,f2,f3,f4}.prompt.md`. Обязательные блоки каждого ТЗ: (а) файловые границы пакета — правки ТОЛЬКО в своих `specs/<t>/*` и `context/<t>.md`, транскрипты `*-interview.md` не трогать; (б) AGR-019/020/021 дословно; (в) правило «контракт чинит владелец / потребитель выравнивается под текущий текст владельца» (для F3/F4 — «перечитай текст владельца ПОСЛЕ волны 1, не цитаты ревью»); (г) принятые отклонения круга 2 (7 пунктов из run3.sh); (д) верификационная батарея + грабли red-якорей из notes (симуляция по конвенциям репо, `sys.path.insert`); (е) право отклонить находку только с якорной цитатой-доказательством; (ж) финальный отчёт через SendMessage оркестратору: per-finding статус fixed/rejected+why.
- В ТЗ F1 добавить D-14/AGR-021 (S4-C2: ворнинг `unconfirmed_ice=<список>` в ordering-выводе + эскалация подтверждения пользователю, флип `ice_confirmed:true` после подтверждения; порядок не блокируется); в ТЗ F3 — AGR-020 (Cursor в полный режим S3 наравне с Pi/OpenCode, трассировка на D-07).

**Testing (TDD):**
- Самопроверка пакетов кодом: `jq`-сверка — сумма находок в 4 промптах = 73 техн. + 2 смысловых, без дублей и потерь (скрипт-однострочник по id).

**DoD (SMART):**
- [x] 4 файла `fix3-f{1..4}.prompt.md` существуют; jq-сверка: каждая из 75 находок ровно в одном пакете
- [x] Каждый промпт содержит блоки (а)–(ж); AGR-020/021 процитированы дословно из `agreements.md`
- [x] Спеки на этой стадии не изменены (`git diff --stat` по `specs/` пуст относительно начала стадии)

**Code rules:** KISS — ТЗ переиспользует готовые proposed_fix из вердиктов; DRY — общие блоки один раз в шаблоне.

---

<!-- mb-stage:2 -->
### Stage 2: Волна 1 — F1 owners-state (umbrella + S4) на Opus

**What to do:**
- Спавн Agent (model: opus) с `fix3-f1.prompt.md`: 18 находок umbrella/S4 (включая critical PARTIAL R2-007 mv-reclaim двух писателей и PARTIAL R2-003 ordering-контракт) + приземление AGR-021.
- Запускается параллельно со Stage 3 (пакеты не пересекаются по файлам).
- По завершении — независимая верификация оркестратором: батарея по обеим спекам + перепрогон изменённых red-эвалов + двусторонняя симуляция изменённых якорей + сверка каждой находки пакета со статусом из отчёта фиксера.

**Testing (TDD — батарея как тесты спеко-правок):**
- `bash scripts/mb-ears-validate.sh` по requirements обеих спек
- `bash scripts/mb-spec-validate.sh .memory-bank/specs/sdd-vision-pipeline` (базовый) и `--require-scenarios` для `svp-roadmap-backlog-db`
- `python3 scripts/mb-scenario-extract.py` (паритет сценариев, test_id уникальны/ASCII); `scripts/mb_work_items.py` (парсер задач)
- Изменённые Eval: as-is → exit≠0 И якорь НЕ матчится; симуляция настоящего провала по конвенциям репо → матчится

**DoD (SMART):**
- [x] Все 18 находок F1 + D-14: fixed либо rejected с якорной цитатой; отчёт фиксера получен и сверен построчно
- [x] Батарея umbrella + S4 зелёная; изменённые якоря прошли двустороннюю проверку
- [x] AGR-021 в тексте S4-C2/Interface 4: ворнинг + эскалация + флип-протокол; `ice_confirmed` больше не декоративен
- [x] Правки только в файлах пакета F1 (`git diff --name-only`)

**Code rules:** contract-first — сначала текст контракта владельца, потребители ждут волну 2.

---

<!-- mb-stage:3 -->
### Stage 3: Волна 1 — F2 owners-norms (S2 + S1) на Opus

**What to do:**
- Спавн Agent (model: opus) с `fix3-f2.prompt.md`: 15 находок S2/S1, включая critical S1 R3-001 (секрет в `<private>` попадает в git — правка политики транскриптов: redaction ДО записи в git-tracked файл) и critical-PARTIAL S2 F-010 (red/green фальсифицируем флагами — закрыть через work-state seam).
- Параллельно Stage 2; той же процедурой верификации по завершении.

**Testing (TDD):**
- Батарея по S2 и S1 (`--require-scenarios`); паритет сценариев; парсер задач
- Изменённые Eval/якоря — двусторонняя проверка как в Stage 2

**DoD (SMART):**
- [x] Все 15 находок F2: fixed либо rejected с цитатой; отчёт сверен построчно
- [x] Батарея S2 + S1 зелёная; изменённые якоря двусторонне проверены
- [x] Политика секретов S1 не допускает plaintext-секрет в git-tracked транскрипт (текст требования + сценарий + Eval)
- [x] Правки только в файлах пакета F2

**Code rules:** нормы S2 — владельческие: формулировки REQ-054/055 меняются только здесь, потребители (S8 и все Eval) выравниваются в волне 2.

---

<!-- mb-stage:4 -->
### Stage 4: Волна 2 — F3 consumers-west (S7 + S3 + S5) на Opus

**What to do:**
- Старт ТОЛЬКО после верификации Stages 2–3. Спавн Agent (model: opus) с `fix3-f3.prompt.md`: 23 находки + AGR-020 (Cursor в полный режим S3: scope-строка, dispatch-таблица, трассировка D-07; выравнивание под текущие контракты S4-C2/C6 и S2 после волны 1).
- Ключевые: S7 R3-002 (4 якоря принимают foreign failure за red), S5 R3-006 (GNU `timeout` нереализуем на стоковом macOS — заменить POSIX-совместимой конструкцией), PARTIAL-цепочка S3←S4/S5.
- Параллельно Stage 5; верификация оркестратором по завершении.

**Testing (TDD):**
- Батарея по S7/S3/S5 (`--require-scenarios`); паритет сценариев; парсер задач
- Cross-slice grep: каждая ссылка S3/S5/S7 на контракты S4/S2/S1 совпадает с текущим текстом владельца (обе стороны)
- Изменённые якоря — двусторонняя проверка

**DoD (SMART):**
- [x] Все 23 находки F3: fixed либо rejected с цитатой; отчёт сверен построчно
- [x] AGR-020 приземлён: S3 объявляет полный режим для Claude Code/Pi/OpenCode/Cursor с трассировкой D-07
- [x] Батарея S7 + S3 + S5 зелёная; cross-slice grep без расхождений; якоря двусторонне проверены
- [x] Правки только в файлах пакета F3

**Code rules:** потребитель не переписывает контракт владельца — расхождение с волной 1 = вопрос оркестратору, не самодеятельная правка.

---

<!-- mb-stage:5 -->
### Stage 5: Волна 2 — F4 consumers-east (S6 + S8) на Opus

**What to do:**
- Старт после верификации Stages 2–3. Спавн Agent (model: opus) с `fix3-f4.prompt.md`: 17 находок, включая 3 critical: S6 R3-001 (bootstrap всегда `stale_run` — определить первый прогон), S6 R3-002 (apply не привязан к авторитетному plan — SHA и deprecate/degraded_sources переносятся из plan, не от LLM), S8 R3-001 (дефолт `layers=true` ломает легаси-спеки — grandfather-правило/миграция).
- Параллельно Stage 4; верификация оркестратором по завершении.

**Testing (TDD):**
- Батарея по S6/S8 (`--require-scenarios`); паритет сценариев; парсер задач
- Cross-slice grep: ссылки S6→S4(lock)/S2 и S8→S2(нормы, `--eval-status` enum) совпадают с текстом владельца
- Изменённые якоря — двусторонняя проверка

**DoD (SMART):**
- [x] Все 17 находок F4: fixed либо rejected с цитатой; отчёт сверен построчно
- [x] Легаси-правило S8: существующие спеки без `layers:` остаются валидными (негативный пример в spec-тексте)
- [x] Батарея S6 + S8 зелёная; cross-slice grep без расхождений; якоря двусторонне проверены
- [x] Правки только в файлах пакета F4

**Code rules:** честная деградация вместо тихого обхода (bootstrap/fallback-исходы наблюдаемы в выводе).

---

<!-- mb-stage:6 -->
### Stage 6: Финальный централизованный проход и актуализация банка

**What to do:**
- Полная батарея всех 9 спек + сквозной cross-slice grep всех межслайсовых контрактов (обе стороны) + спот-чек якорей, менявшихся в обеих волнах.
- Свести per-finding итог (75 строк: fixed/rejected+why) в `reports/2026-07-18_review_spec-group-round3-remediation.md`.
- Актуализировать банк: `roadmap.md` (блок круга 3 + итог ремедиации), `status.md`, `checklist.md`, `progress.md` (append), при новых граблях — `notes/`.
- НЕ коммитить без явного запроса пользователя (в дереве чужие правки; при коммите — scoped `git add`).

**Testing (TDD):**
- Батарея 9/9: EARS, spec-validate (umbrella базовый, 8 child `--require-scenarios`), паритет сценариев, парсер задач, red-эвалы с якорями

**DoD (SMART):**
- [x] Батарея 9/9 зелёная одним прогоном; лог прогона сохранён в отчёт ремедиации
- [x] Отчёт ремедиации: 75/75 находок в статусе fixed или rejected-with-evidence, ни одной потерянной
- [x] roadmap/status/checklist/progress актуализированы; изменения не закоммичены
- [x] Пользователю доложено: группа готова к контрольному кругу 4 (запуск — по его команде)

**Code rules:** append-only progress; scoped diff; Response format Goal → Action → Result.

---

## Risks and mitigation

| Risk | Probability | Mitigation |
|------|-------------|------------|
| Фиксер «закрывает» находку, искажая замысел беседы (главный риск круга 3 по опыту аудитора) | M | ТЗ требует цитату источника при любом смысловом изменении; построчная сверка оркестратором против вердикта и леджера |
| Ложная находка ревьюера (codex неправ) | M | Право фиксера на rejected только с якорной цитатой; оркестратор перепроверяет каждое отклонение фактически |
| Пересечение правок общих файлов у параллельных фиксеров | L | Непересекающиеся файловые пакеты, волны по 2, `git diff --name-only`-контроль границ в DoD |
| Регресс межслайсового контракта после правки владельца | M | Порядок волн (владельцы → потребители), «перечитай текущий текст владельца», финальный cross-slice grep обеих сторон |
| Невалидная симуляция red-якоря → ложный брак/приём | M | Конвенции репо в ТЗ (`sys.path.insert`, notes-грабли), двусторонняя проверка каждого изменённого якоря |
| Silent-idle сабагента без отчёта | L | Явный SendMessage-контракт в ТЗ; при молчании — запрос отчёта (урок в нативной памяти) |

## Gate (plan success criterion)

Все 75 находок круга 3 закрыты (fixed) или отклонены с якорным доказательством (rejected-with-evidence), полная верификационная батарея 9/9 спек зелёная одним прогоном, AGR-020/021 фактически приземлены в текстах S3/S4/umbrella, банк актуализирован — группа `sdd-vision-pipeline` готова к контрольному кругу 4 по команде пользователя.
