# Interview transcript: svp-docs-wiki (2026-07-17, self-interview)

Слайс S6 группы `sdd-vision-pipeline`. Режим: **self-interview (D-04)**; assumptions S6-A-01…05 подтверждены пользователем пакетом («да, все так», 2026-07-17). Родительская формулировка фичи (D-27/28) — от пользователя дословно в `context/sdd-vision-pipeline-interview.md` («LLM wiki Карпатого… папка docs по умолчанию, меняется в pipeline.yaml… по команде /mb docs… анализируем диф за незафиксированные сессии»); гист изучён при фиксации D-27.

## Self-answered

- SHA-стейт в docs-папке — отклонён `<bank>/tmp/` (не переживает клон/переезд вики).
- Bootstrap полным обзором на первом запуске — отклонён отказ без истории (пустая вики бесполезна).
- Структура index/log/pages — прямое следование гисту; отклонена плоская папка без index (ломает query-операцию).
- Сабагентный диспатч по образцу /mb wiki — отклонён прямой API-вызов (ключ, стоимость).
- docs.path в pipeline.yaml — отклонена env-переменная (конфиг должен жить в версионируемом файле).

## Финальный гейт

Пакет подтверждён без правок.
