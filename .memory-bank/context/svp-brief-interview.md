# Interview transcript: svp-brief (2026-07-17, self-interview)

Слайс S7 группы `sdd-vision-pipeline`. Режим: **self-interview (D-04)** — агент сам ответил на слайс-специфичные вопросы; assumptions S7-A-01…04 предъявлены пользователю пакетом (вместе с S2–S6) и **подтверждены** («да, все так», 2026-07-17). Родительский ход обсуждения — `context/sdd-vision-pipeline-interview.md` (D-34: формулировка фичи целиком от пользователя, включая референсы 1P Авито / 2ГИС / Amazon и состав одностраничника).

## Self-answered

- Где хранить → `briefs/<topic>/` + `inputs/` (реюз конвенции inputs-registry). Отклонено: класть в `context/` (смешивает стадии), внешняя папка вне банка (теряет git/поиск).
- Формат → 9 секций из формулировки пользователя. Отклонено: свободная форма (не валидируется скриптом).
- Порог вопросов → ≤5 и только про суть/цель. Отклонено: полноценное мини-интервью (дублирует discuss).
- Хендоф → discuss Phase 0 читает бриф автоматически. Отклонено: ручная передача ссылкой (теряется).

## Финальный гейт

Пакет assumptions подтверждён пользователем без правок.
