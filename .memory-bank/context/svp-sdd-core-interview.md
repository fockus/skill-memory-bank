# Interview transcript: svp-sdd-core (2026-07-17, self-interview)

Слайс S2 группы `sdd-vision-pipeline`. Режим: **self-interview (D-04)**; assumptions S2-A-01…06 подтверждены пользователем пакетом («да, все так» + отдельное подтверждение шестого пункта про D-35, 2026-07-17). Родительские решения — `context/sdd-vision-pipeline-interview.md` (Q2 UX sdd, Q3 план в tasks.md, Q4-голос про контракт-ферст без кода на SDD, Q5 gated-охват, Q8 бюджеты с поправкой этап≤400k, D-30 spec-review, D-35 эскалация на генерации — формулировка пользователя: «убедись что команда sdd… проверяла на объем задачи и эскалировала проблему на пользователя, если спека большая»).

## Self-answered

- Реализация конвейера в prompt-слое commands/sdd.md + mb-sdd.sh — отклонён Python-оркестратор (тяжело, ломает кросс-платформенность prompt-команд).
- v2-поля как bold-строки в существующих mb-task блоках — отклонён YAML-блок внутри задач (два синтаксиса в одном файле, хрупкий парсинг).
- spec_review конфиг зеркалит review-контракт work (JSON, severity) — отклонён свободный текст вердикта (не гейтится детерминированно).
- Бюджет-валидация расширением mb-estimate-check.sh — отклонён отдельный скрипт (дублирование рубрики).
- S2 не разбивается (~700–900k) — отклонено превентивное разбиение (ниже порога D-10).

## Финальный гейт

Пакет подтверждён; шестой пункт (D-35) добавлен пользователем явно.
