# Interview transcript: svp-interview-upgrade (2026-07-17, JIT-интервью слайса S1)

Короткое JIT-интервью слайса группы `sdd-vision-pipeline` — входной контекст для планировщика и spec-ревьювера (D-29). Родительские решения D-01…D-31 унаследованы из `context/sdd-vision-pipeline.md` и не переспрашивались; полный ход родительского интервью — `context/sdd-vision-pipeline-interview.md`.

## Унаследовано (не обсуждалось повторно)

D-04 (self-interview), D-09 (финальный гейт), D-10 (размерный триаж ~1M + реестр), D-11 (качество-дефолт + escape-hatch), D-12 (план интервью + сверка закрытия), D-18 (batch-опция), D-22 (глоссарий минимально), D-29 (транскрипт), D-31 (группы спек). ICE слайса 8×9×7=504 подтверждён пользователем при утверждении umbrella-спеки.

## Q&A

**Q1 (оценка объёма).** Как оценивать объём темы в токенах во время интервью для триггера разбиения при >~1M, если код ещё не написан?
**A1.** «LLM по рубрике + скрипт-валидация (Recommended)» → **S1-D-01**. Отклонено: чистая LLM-оценка (непроверяемо, противоречит D-14), формула по числу REQ (REQ разновесны, оценка доступна слишком поздно).

**Q2 (приватность).** Транскрипт пишет ответы близко к дословному и попадает в git — как обрабатывать секреты/PII?
**A2.** «<private>-механизм + secret-scan (Recommended)» → **S1-D-02**. Отклонено: транскрипт вне git (ломает D-29), запись без обработки (утечка в git-историю).

**Рекомендации без вопросов (пользователь не возразил):**
- **S1-D-03** — interview-plan в `<bank>/tmp/`, шаблон с секцией «Унаследованные решения», резюм при отмене.
- **S1-D-04** — assumptions: интерактив — пакет на подтверждение до генерации; auto — без блокировки, ревизия после.
- **S1-D-05** — `--batch` через AskUserQuestion (до 4/вызов), деградация в нумерованный текст на других хостах.
- **S1-D-06** — `glossary.md`: «термин — определение», inline-обновление, челлендж конфликтов.

**Финальный гейт.** «Есть ли что добавить по слайсу S1?»
**Ответ.** «Все ok» — дополнений нет, сводка S1-D-01…06 подтверждена.
